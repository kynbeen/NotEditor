services:
  - type: web
    name: noteditor
    runtime: docker
    plan: free
    region: singapore
    healthCheckPath: /api/health
    maxShutdownDelaySeconds: 300
    envVars:
      - key: NOTEDITOR_MAX_UPLOAD_MB
        value: "100"
      - key: NOTEDITOR_SESSION_TTL
        value: "3600"
