순서를 바꾸고, 쪽 매칭까지 바꾸는 기능을 넣자.

추가 질문이 있으면 여기와 채팅창 모두에 해.

## 2026-09-04 추가 질문

1. PDF 자유 순서를 summary.ai에서 넘어온 합치기에도 허용할까요? 그러려면
   `C:\dev\summary.ai`의 사이드카와 재현 규격도 함께 확장해야 합니다. 아니면 일반
   NotEditor에서만 자유 순서를 제공하고 summary.ai 인계는 현재 추적 가능한 순서를 유지합니다.
2. 데스크톱에서 쓰는 PyMuPDF에 대해 Artifex 상용 라이선스를 보유하고 있나요? 없다면 MIT
   배포를 유지할 수 있도록 데스크톱 PDF 읽기와 렌더링도 허용적인 라이선스 구현으로 교체합니다.

summary.ai에서도 허용하는 건 상관 없는데, 일관성 유지하는 게 나은지 아니면 그냥 지금을 유지하는 게 나은지 유지보수면에서 판단해보고 더 이득인 쪽으로 가봐.

상용 라이선스는 없어. 근데 어차피 개인적으로 쓸 거면(또는 상업화하지 않는다면) 상관 없는 거 아니야?

## 2026-09-05 확인 및 진행

- 개인 기기에서 혼자 사용하는 경우 상용 라이선스가 필수는 아닙니다. 다만 비상업적 배포도
  배포이므로 AGPL 조건이 없어지지는 않습니다. 데스크톱 PyMuPDF는 유지하며, README에
  자체 코드의 MIT와 의존성의 AGPL/상용 라이선스를 구분했습니다.
- summary.ai 자유 순서는 유지보수 관점에서 판단하도록 위임받았습니다. 기존 결과 추적과
  재현 호환성을 확인한 뒤 구현 범위를 결정합니다.

## 2026-09-05 Goodnotes 목차 질문

Goodnotes 원본에 이미 목차가 있을 때 입력한 JSON 목차는 어떻게 적용할까요?

1. 기존 목차를 유지하고 JSON 항목을 추가합니다.
2. JSON을 입력한 경우에만 기존 목차를 교체합니다.

JSON을 입력하지 않은 경우에는 기존 목차를 보존하는 방향으로 진행합니다.
현재 추가한 코드는 새 JSON 목차의 페이지 대응·저장 검증이며, 기존 목차의 보존·병합은 아직 미완료입니다.

중간중간 핸드오프도 해. 컨텍스트 계속 압축하면 맥락 손실이 생기지 않아? 기본적으로 작동할 때 devkit을 적극적으로 활용해.